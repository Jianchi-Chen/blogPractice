# SQLite file path
DATABASE_URL=sqlite://app.db

# HTTP listener
HOST=127.0.0.1
PORT=3000

# Required: use at least 32 random bytes and never commit the real value
JWT_SECRET=
JWT_TTL=604800

RUST_LOG=info,axum_sqlx_sqlite_skeleton=debug
