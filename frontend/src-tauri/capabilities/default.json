{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "local": true,
    "description": "enables the default permissions",
    "windows": ["main"],
    "permissions": [
        "core:default"
    ]
}
